// Penjelasan file: BuMaraShelfMinigameView.swift
// File mirror / alias untuk BuMaraShelfMinigameView.

import SwiftUI

// Implementasi utama berada di ShelfBalanceMinigame.swift.
// File ini memastikan jika developer membuka atau mengimpor BuMaraShelfMinigameView.swift secara langsung,
// semua tipe dan preview tersedia dengan lengkap.

#if canImport(SwiftUI) && DEBUG
#Preview("Bu Mara Shelf Minigame View", traits: .landscapeLeft) {
    BuMaraShelfMinigameView()
}
#endif
